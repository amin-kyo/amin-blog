---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

const pageTitle = `About - ${SITE_TITLE}`;
const pageDescription = '技術メモ、趣味の記録、活動ログを発信するブログの運営者について';

const techStack = {
  production: [
    { name: 'HTML', level: 3, note: 'わりと好き' },
    { name: 'CSS', level: 3, note: '好き' },
    { name: 'JavaScript', level: 2, note: 'ちょっと' },
    { name: 'kintone', level: 3, note: 'ちょっとわかる' },
    { name: 'VBA', level: 2, note: '触りたくはない' },
  ],
  learning: [
    { name: 'Astro', level: 2, note: 'このブログで触ってる' },
    { name: 'React', level: 1, note: '触ってる' },
    { name: 'Node.js', level: 1, note: '雰囲気' },
    { name: 'AWS', level: 1, note: 'むずかしい' },
    { name: 'GitHub', level: 2, note: 'このブログのソース管理' },
    { name: 'Cloudflare Pages', level: 2, note: 'このブログのホスティング' },
    { name: 'Generative AI', level: 1, note: 'おともだち' },
    { name: 'PostgreSQL', level: 1, note: 'データは入れられる' },
  ],
};

const profileData = [
  { label: 'Role', value: '企画屋さん' },
  { label: 'Experience', value: '下水処理場オペレーター → 企画・DX推進' },
  { label: 'Specialties', value: '企画・提案、調整、従業員教育、動画・資料制作、開発' },
];

const certifications = [
  '基本情報技術者',
  '下水道第3種技術検定',
  'STREET FIGHTER 6 ED Master',
];

const interests = {
  tech: ['Webデザイン', 'UI/UX', 'CSS', 'IT分野全般', 'ライブ配信'],
  hobbies: ['PC・ガジェット', 'ビデオゲーム', 'クラフトビール','ウイスキー','ダーツ', '歌','インテリア', '自転車', 'キャンプ'],
};


const renderStars = (level: number) => {
  return '⭐'.repeat(level);
};
---

<!DOCTYPE html>
<html lang="ja">
  <head>
    <BaseHead title={pageTitle} description={pageDescription} />
  </head>
  <body>
    <Header />
    
    <main class="container">
      <!-- ヒーロー -->
      <header class="page-header">
        <h1 class="page-title gradient-text">About</h1>
      </header>

      <!-- Overview セクション -->
      <section class="card card-padding-lg mb-8">
        <h2 class="section-title">Overview</h2>
        <div class="overview-content">
          <p class="text-lg text-secondary mb-4">あみんのメモ、あみめもです。</p>
        </div>
      </section>
      
      <!-- Profile セクション -->
      <section class="card card-padding-lg mb-8">
        <h2 class="section-title">Profile</h2>
        <div class="profile-grid">
          {profileData.map((item) => (
            <div class="profile-item">
              <dt class="profile-label font-bold">{item.label}</dt>
              <dd class="profile-value text-secondary">{item.value}</dd>
            </div>
          ))}
        </div>
      </section>
      
      <!-- Tech Stack セクション -->
      <section class="card card-padding-lg mb-8">
        <h2 class="section-title">Tech Stack</h2>
        
        <div class="tech-section mb-8">
          <h3 class="tech-category">Production</h3>
          <div class="grid grid-auto-fit-sm gap-4">
            {techStack.production.map((tech) => (
              <div class="tech-item">
                <div class="tech-header">
                  <span class="tech-name">{tech.name}</span>
                  <span class="tech-stars">{renderStars(tech.level)}</span>
                </div>
                <span class="tech-note">{tech.note}</span>
              </div>
            ))}
          </div>
        </div>
        
        <div class="tech-section">
          <h3 class="tech-category">Learning</h3>
          <div class="grid grid-auto-fit-sm gap-4">
            {techStack.learning.map((tech) => (
              <div class="tech-item">
                <div class="tech-header">
                  <span class="tech-name">{tech.name}</span>
                  <span class="tech-stars">{renderStars(tech.level)}</span>
                </div>
                <span class="tech-note">{tech.note}</span>
              </div>
            ))}
          </div>
        </div>
      </section>
      
      <!-- Skills セクション -->
      <section class="card card-padding-lg mb-8">
        <h2 class="section-title">Skills & Certifications</h2>
        <div class="grid grid-auto-fit-sm gap-4">
          {certifications.map((cert) => (
            <div class="cert-item">
              <span class="cert-icon">🏆</span>
              <span class="cert-name">{cert}</span>
            </div>
          ))}
        </div>
      </section>
      
      <!-- Interests セクション -->
      <section class="card card-padding-lg mb-8">
        <h2 class="section-title">Interests</h2>
        <div class="grid grid-auto-fit gap-8">
          <div class="interest-category">
            <h3 class="interest-title">Tech</h3>
            <div class="interest-tags">
              {interests.tech.map((item) => (
                <span class="interest-tag tech-tag">{item}</span>
              ))}
            </div>
          </div>
          <div class="interest-category">
            <h3 class="interest-title">Hobbies</h3>
            <div class="interest-tags">
              {interests.hobbies.map((item) => (
                <span class="interest-tag hobby-tag">{item}</span>
              ))}
            </div>
          </div>
        </div>
      </section>
      
      
      <!-- Contact セクション -->
      <section class="card card-padding-lg">
        <h2 class="section-title">Contact</h2>
        <div class="text-center">
          <div class="contact-links">
            <a href="https://mstdn.jp/@amin" target="_blank" rel="me" class="contact-link mastodon">
              <span class="contact-icon">🐘</span>
              Mastodon
            </a>
            <a href="https://github.com/amin-kyo" target="_blank" class="contact-link github">
              <span class="contact-icon">🐙</span>
              GitHub
            </a>
          </div>
        </div>
      </section>
    </main>
    
    <Footer />
  </body>
</html>